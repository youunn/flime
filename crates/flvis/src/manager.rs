use std::collections::HashMap;

pub struct GraphicsManager {
    pipelines: Pipeline,
    bufs: HashMap::<usize, Vec<Buffer>>,
}

pub struct Pipeline {}

impl GraphicsManager {
    fn run() {
    
    } 
}